# TEXT FORMATER

Formata textos para ocultar dados sensíveis

## Pré-Requisitos

- Java >= 8.0

## Execução

Compile o código executando o comando no terminal.

```bash
javac Main.java TextFormater.java
```

Em seguida execute o arquivo principal (Main) no terminal

```bash
java Main
```